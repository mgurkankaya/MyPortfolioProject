@model List<PortfolioProject.Models.TlbProjects>
<div class="portfolio" id="portfolio">
    <div class="container">
        <div class="section-header text-center wow zoomIn" data-wow-delay="0.1s">
            <p>Projeler</p>
            <h2>Tamamladığım Projeler</h2>
        </div>
        <div class="row">
            <div class="col-12">
                <ul id="portfolio-filter">
                    <li data-filter="*" class="filter-active">Tümü</li>
                   @foreach(var item in ViewBag.categories as List<PortfolioProject.Models.TblCategories>)
                   {
                    <li data-filter=".@item.CategoryId">@item.CategoryName</li>
                   }
                </ul>
            </div>
        </div>
        <div class="row portfolio-container">
           @foreach(var item in Model)
           {


            <div class="col-lg-4 col-md-6 col-sm-12 portfolio-item @item.CategoryId wow fadeInUp" data-wow-delay="0.0s">
                <div class="portfolio-wrap">
                    <div class="portfolio-img">
                        <a target="_blank" href="@item.GithubUrl"><img src="@item.ImageUrl" alt="Image"></a>    
                    </div>
                    <div class="portfolio-text">
                        <h3>@item.ProjectName</h3>
                        <a class="btn" href="@item.GithubUrl" data-lightbox="portfolio">+</a>
                    </div>
                </div>
            </div>
           }
        </div>
    </div>
</div>
